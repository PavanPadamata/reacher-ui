# Copy this to .env.local for local development (without Docker Compose)

# URL of the Reacher backend
# In Docker Compose, this is set automatically to http://reacher:8080
REACHER_BACKEND_URL=http://localhost:8080

# Public URL of this app (used for metadata)
NEXT_PUBLIC_APP_URL=http://localhost:3000
